# Wikipedia Movies Retrieval System - Requirements
# 
# Python version: 3.10+ (for type hints and modern features)
#
# Core dependencies:
# - pandas: Used in main.py for data loading and manipulation
# - nltk: Natural Language Toolkit for professional tokenization, stemming, stopwords
# - kagglehub: Used in download_dataset.py to fetch datasets from Kaggle
#
# Components analysis:
# - Components/Tokenizer.py: Uses NLTK (word_tokenize, PorterStemmer, stopwords) following IIR best practices
# - Components/Indexer.py: Only uses built-in modules (os, pickle, hashlib, collections)
# - Components/QueryProcessor.py: Only uses built-in modules (math, collections)
# - indexer_validation.ipynb: Uses pandas for notebook testing
#
# All other imports are Python standard library (no external dependencies needed)

# Data manipulation and analysis
pandas>=2.0.0

# Natural Language Processing (IIR best practices)
nltk>=3.8.0

# Dataset download from Kaggle (optional - only needed if downloading dataset)
kagglehub>=0.2.0

